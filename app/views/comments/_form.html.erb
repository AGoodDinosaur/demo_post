
<div class="comment">
  <%= simple_form_for([@post, @post.comments.build]) do |f| %>
    <%= f.input :comment , input_html: { class: 'texterea'}, wrapper: false, label_html: {class: 'label'}%>
    <br />
    <br />
    <%= f.button :submit , class: "button is-primary" %>
  <% end %>
</div>